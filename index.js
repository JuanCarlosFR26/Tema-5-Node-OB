let alturaCM = 180;
let alturaMetros = 1.80;
let pesoK = 80.0;
let alturaR = Math.ceil(alturaMetros);
let pesoR = Math.floor(pesoK);

if(Math.max() === (Math.max() + 1)) {
    console.log("Verdadero");
}